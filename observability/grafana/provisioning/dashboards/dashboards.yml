# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: "POC Lab Dashboards"
    orgId: 1
    folder: "Redis POC Lab"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false

