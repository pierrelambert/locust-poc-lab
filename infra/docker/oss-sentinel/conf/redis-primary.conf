# Redis primary configuration — OSS Sentinel stack
# Persistence: AOF every 1 sec (appendfsync everysec)

bind 0.0.0.0
protected-mode no
port 6379

# Persistence
appendonly yes
appendfsync everysec
save ""

# Memory
maxmemory-policy noeviction

# Logging
loglevel notice

